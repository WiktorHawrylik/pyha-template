# Copilot Instructions

- Follow AGENTS.md for mandatory guardrails.
- Use the src/ layout (`src/python_library_template`).
- Run `ruff`, `mypy`, and `pytest` for changes that affect code.
- Keep changes minimal and update docs for public API changes.
- Use Conventional Commits for commit messages.
