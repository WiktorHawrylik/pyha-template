"""Core helpers for the template package."""


def greet(name: str) -> str:
    """Return a friendly greeting.

    Args:
        name: Name to greet.

    Returns:
        A greeting string.
    """
    return f"Hello, {name}!"
